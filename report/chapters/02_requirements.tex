To construct a valuable tool for shared living, a rigorous requirements analysis was conducted. The requirements are broken down into Functional Requirements (actions the system must perform) and Non-Functional Requirements (qualities the system must possess).

\section{Functional Requirements}
The functional requirements are structured around the core modules of the application: user authentication and profile management, household management, financial tracking, chore coordination and shopping list management.

\subsection{User Authentication and Profile Management}
\begin{itemize}
    \item \textbf{Registration \& Login (FR-1):} The system shall allow users to register an account by providing a name, surname, email, and password. Upon successful registration, the system must return a valid authentication token, allowing immediate access within the session. Registered users must be able to log in using their email and password.
    \item \textbf{Profile Management (FR-2):} Authenticated users must be able to view and update their profile information, including optional financial details such as IBAN and a payment link, to facilitate real-world debt settlements among housemates.
\end{itemize}

\subsection{Household Management}
\begin{itemize}
    \item \textbf{Create Household (FR-3):} An authenticated user who does not already belong to a household must be able to create a new one by specifying a unique household name. The creating user is automatically assigned the administrator role for that household, and a unique invitation code is generated upon creation.
    \item \textbf{Join Household (FR-4):} A user who does not belong to any household must be able to join an existing one by providing a valid invitation code. The user is added as a standard (non-admin) member.
    \item \textbf{Members Overview (FR-5):} Users must be able to view the full list of current members belonging to their household, including each member's profile information and administrative role status.
    \item \textbf{Invitation Code Management (FR-6):} Users must be able to retrieve the current invitation code for their household. Household administrators must be able to refresh the invitation code, invalidating the previous one to control access.
    \item \textbf{Remove Member (FR-7):} A household administrator must be able to remove any of its members.
    \item \textbf{Leave Household (FR-8):} A user must be able to voluntarily leave their current household. If the leaving user is the sole administrator and other members remain, the system must automatically transfer the admin role to another member. If the leaving user is the last member, the household is deleted.
\end{itemize}

\subsection{Financial Tracking}
\begin{itemize}
    \item \textbf{Log Expense (FR-9):} A household member shall be able to register a new expense by specifying a description, a total amount, the list of involved household members and a split type (which can be equal or unequal). The system must compute each user's individual share according to the selected split strategy and persist the expense.
    \item \textbf{Expenses Overview (FR-10):} The system must provide a household-level expense overview reporting the total amount and count of expenses for the current calendar month, also providing support for dynamic expense filtering. Additionally, users must be able to retrieve a personal net cash-flow summary for the current month.
    \item \textbf{Automatic Debt Calculation (FR-11):} Upon logging an expense, the system must automatically compute and update the debt ledger, creating or adjusting debt records between the payer and each participant via a netting algorithm that simplifies complex interdependent debts into streamlined balances.
    \item \textbf{Debts Overview (FR-12):} Users must be able to view their pending debts, filtered by transaction role (creditor, debtor or both) and, optionally, by counterpart. The system must also provide an aggregate overview showing the total amounts owed by and to the user.
    \item \textbf{Settle Debt (FR-13):} A user who has debts with another user must be able to log a settlement payment specifying the settled amount and an optional description. The system must validate that the settlement amount does not exceed the current debt balance and reduce the balance accordingly.
    \item \textbf{Settlements Overview (FR-14):} Users must be able to view the history of settlement transactions within their household, with support for dynamic filtering.
\end{itemize}

\subsection{Chore and Task Management}
\begin{itemize}
    \item \textbf{Create Chore (FR-15):} A household member must be able to define a new chore by specifying a description and a recurrence frequency in days.
    \item \textbf{Assign Chore (FR-16):} Users must be able to create an assignment for an existing chore, specifying the assigned user and an optional due date.
    \item \textbf{Chores Overview (FR-17):} Users must be able to view all chores defined within their household and retrieve filtered lists of chore assignments. The system must also provide an assignment overview summarizing total, completed, pending, and overdue counts both at the household level and for the individual user.
    \item \textbf{Delete Chore (FR-18):} Users must be able to delete a chore (which cascades the removal of all its assignments) or delete an individual assignment.
\end{itemize}

\subsection{Shopping List Management}
\begin{itemize}
    \item \textbf{Create Shopping List (FR-19):} A household member must be able to create a shared shopping list containing a set of items.
    \item \textbf{Update Shopping List (FR-20):} Users must be able to mark individual items within a shopping list as bought or not bought, and also modify the items list content itself. The system must automatically update the list's overall status: \texttt{NOT\_STARTED} if no items are bought, \texttt{IN\_PROGRESS} if some items are bought, and \texttt{COMPLETED} if all items are bought.
    \item \textbf{Shopping Lists Overview (FR-21):} Users must be able to retrieve all shopping lists belonging to their household, including each list's name, items with their bought status, overall list status, and creation date.
    \item \textbf{Delete Shopping List (FR-22):} Users must be able to delete a shopping list from their household.
\end{itemize}

\section{Non-Functional Requirements}
\begin{itemize}
    \item \textbf{Security (NFR-1):} All API endpoints, except registration and login, must be protected from unauthorized accesses using JSON Web Tokens (JWT). The system must verify the token's validity and extract the user identity on every request via a dedicated authentication filter. User passwords must be hashed using a strong one-way algorithm (BCrypt) before being persisted to the database.
    \item \textbf{Maintainability (NFR-2):} The system must be structured following established object-oriented design principles to allow future extension of capabilities with minimal coupling. The codebase must separate concerns into distinct layers: controllers for REST API HTTP requests handling, backend services for business logic and frontend clients to translate users actions into HTTP requests. While the primary client is a JavaFX desktop application, the API-first design must allow the future development of web or mobile clients without backend modifications.
    \item \textbf{Reliability (NFR-3):} The backend must handle database transaction errors gracefully by employing Spring's \texttt{@Transactional} annotation to ensure atomicity (either all needed records are persisted or none are, preventing corrupted household states).
    \item \textbf{Data Validation (NFR-4):} All incoming external data must be validated at the API controller level using Jakarta Bean Validation annotations. This ensures that invalid or malicious input is rejected with informative error responses before reaching the service layer. To mitigate risks from internal service-to-service communication, which may bypass the API boundary, each backend service must independently re-validate its inputs. This two-layer security approach guarantees that every component remains resilient against invalid or malicious data, regardless of whether the request originates from an external client or an internal system.
    \item \textbf{Logging and Observability (NFR-5):} The system must log all significant operations (resource creation, deletion, authentication attempts and errors) at the appropriate severity level, using a structured logging framework, to support debugging and operational monitoring.
\end{itemize}

\chapter{Use Case Diagrams and Templates}
This chapter provides an extensive analysis of the use cases derived from the requirements specified previously, divided in core modules.

\section{User Authentication and Profile Management}

\subsection{Use Case Diagram}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    usecase/.style={
        ellipse, draw=black, fill=gray!20,
        minimum width=3.8cm, minimum height=1.3cm,
        align=center, font=\small\sffamily
    },
    sysbox/.style={
        rectangle, draw=black, rounded corners=3pt,
        minimum width=10cm, minimum height=9cm
    },
    relation/.style={thick, draw=black},
    include/.style={->, dashed, thick, draw=black!70}
]

% System boundary
\node[sysbox, label={[font=\sffamily\bfseries]above:User Authentication \& Profile Management Subsystem}] (sys) at (0,0) {};

% ---- Stick-figure Actor ----
\begin{scope}[shift={(-7,0)}]
    % Head
    \draw[thick] (0, 0.55) circle (0.2cm);
    % Body
    \draw[thick] (0, 0.35) -- (0, -0.15);
    % Arms
    \draw[thick] (-0.3, 0.2) -- (0, 0.1) -- (0.3, 0.2);
    % Legs
    \draw[thick] (-0.25, -0.5) -- (0, -0.15) -- (0.25, -0.5);
    % Label
    \node[font=\sffamily\bfseries, below] at (0, -0.55) {User};
\end{scope}
\coordinate (actor) at (-7, 0);

% Use cases
\node[usecase] (uc1) at (0, 3)   {Register};
\node[usecase] (uc2) at (0, 1)   {Login};
\node[usecase] (uc3) at (0,-1)   {View Profile};
\node[usecase] (uc4) at (0,-3)   {Update Profile};

% Internal use cases (included)
\node[usecase] (uc5) at (5.5, 2)  {Validate\\Credentials};
\node[usecase] (uc6) at (5.5,-1)  {Generate\\Auth Token};

% Actor connections
\draw[relation] (actor) -- (uc1);
\draw[relation] (actor) -- (uc2);
\draw[relation] (actor) -- (uc3);
\draw[relation] (actor) -- (uc4);

% Include relationships
\draw[include] (uc1) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc5);
\draw[include] (uc2) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc5);
\draw[include] (uc1) -- node[below, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc6);
\draw[include] (uc2) -- node[below, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc6);

\end{tikzpicture}}%
\caption{Use Case Diagram: User Authentication \& Profile Management Subsystem}
\end{figure}

The diagram above illustrates the use cases composing the user authentication and profile management subsystem. The \textit{User} actor interacts directly with four boundary use cases. The two internal use cases, \textit{Validate Credentials} and \textit{Generate Auth Token}, are included automatically by the system during registration and login respectively, and are never invoked directly by the user. \textit{View Profile} and \textit{Update Profile} require a valid authentication token.

\subsection{Use Case Templates}

% ===== UC-1: Register =====
\subsubsection{Register UC Template}
\ucheader{UC \#1: Register}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Create a new user account and receive an authentication token for immediate access. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Unregistered User \\ \hline
\textbf{Pre-conditions} & The user does not have an existing account. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user provides a name, surname, email address, password, and optionally an IBAN.
    \item The system validates that all required fields are non-empty and that the email format is valid (include: \textit{Validate Credentials}).
    \item The system verifies that the email is not already registered.
    \item The system hashes the password and persists the new user record.
    \item The system generates an authentication token for the newly created user (include: \textit{Generate Auth Token}).
    \item The system returns the user profile and the authentication token.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & A new user account is persisted and a valid authentication token is returned. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The email is already registered. The system rejects the request.
    \item[\textbf{A2.}] Required fields are missing or the email format is invalid. The system rejects the request.
    \item[\textbf{A3.}] An IBAN is provided but its format is invalid. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-2: Login =====
\subsubsection{Login UC Template}
\ucheader{UC \#2: Login}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Authenticate with existing credentials and obtain a new session token. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Registered User \\ \hline
\textbf{Pre-conditions} & The user has a registered account. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user provides their email address and password.
    \item The system validates that the fields are non-empty and that the email format is valid (include: \textit{Validate Credentials}).
    \item The system authenticates the credentials against the stored user record.
    \item The system generates a new authentication token (include: \textit{Generate Auth Token}).
    \item The system returns the user profile and the authentication token.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The user receives a valid authentication token for the current session. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The email does not match any registered account. The system rejects the request.
    \item[\textbf{A2.}] The password does not match the stored one. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-3: Manage Profile =====
\subsubsection{Manage Profile UC Template}
\ucheader{UC \#3: Manage Profile}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & View and update personal profile information. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Authenticated User \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated with a valid session token. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor requests their current profile information.
    \item The system retrieves and returns the actor's profile data including name, surname, email, IBAN, and payment link.
    \item The actor provides updated profile fields (name, surname, email, IBAN, or payment link).
    \item The system validates the updated fields and persists the changes.
    \item The system returns the updated profile.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The actor's profile is updated with the new values. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The actor only views their profile without making changes. The flow ends at step 2.
    \item[\textbf{A2.}] Any of the provided data for profile update has an invalid format. The system rejects the update request.
\end{itemize} \\ \hline
\end{longtable}

\section{Household Management}

\subsection{Use Case Diagram}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    usecase/.style={
        ellipse, draw=black, fill=gray!20,
        minimum width=3.8cm, minimum height=1.3cm,
        align=center, font=\small\sffamily
    },
    sysbox/.style={
        rectangle, draw=black, rounded corners=3pt,
        minimum width=10cm, minimum height=13.5cm
    },
    relation/.style={thick, draw=black},
    include/.style={->, dashed, thick, draw=black!70}
]

% System boundary
\node[sysbox, label={[font=\sffamily\bfseries]above:Household Management Subsystem}] (sys) at (0,0) {};

% ---- Stick-figure Actor 1: User / Member ----
\begin{scope}[shift={(-7, 2)}]
    % Head
    \draw[thick] (0, 0.55) circle (0.2cm);
    % Body
    \draw[thick] (0, 0.35) -- (0, -0.15);
    % Arms
    \draw[thick] (-0.3, 0.2) -- (0, 0.1) -- (0.3, 0.2);
    % Legs
    \draw[thick] (-0.25, -0.5) -- (0, -0.15) -- (0.25, -0.5);
    % Label
    \node[font=\sffamily\bfseries, below] at (0, -1) {User / Member};
\end{scope}
\coordinate (user) at (-7, 2);

% ---- Stick-figure Actor 2: Admin ----
\begin{scope}[shift={(-7, -4)}]
    % Head
    \draw[thick] (0, 0.55) circle (0.2cm);
    % Body
    \draw[thick] (0, 0.35) -- (0, -0.15);
    % Arms
    \draw[thick] (-0.3, 0.2) -- (0, 0.1) -- (0.3, 0.2);
    % Legs
    \draw[thick] (-0.25, -0.5) -- (0, -0.15) -- (0.25, -0.5);
    % Label
    \node[font=\sffamily\bfseries, below] at (0, -0.8) {Admin};
\end{scope}
\coordinate (admin) at (-7, -4);

% Use cases
\node[usecase] (uc1) at (0, 5)   {Create Household};
\node[usecase] (uc2) at (0, 3)   {Join Household};
\node[usecase] (uc3) at (0, 1)   {View Members};
\node[usecase] (uc4) at (0,-1)   {Leave Household};
\node[usecase] (uc5) at (0,-3)   {Manage Invitation Code};
\node[usecase] (uc6) at (0,-5)   {Remove Member};

% Actor connections
\draw[relation] (user) -- (uc1);
\draw[relation] (user) -- (uc2);
\draw[relation] (user) -- (uc3);
\draw[relation] (user) -- (uc4);
\draw[relation] (user) -- (uc5);

\draw[relation] (admin) -- (uc5);
\draw[relation] (admin) -- (uc6);

\end{tikzpicture}}%
\caption{Use Case Diagram: Household Management Subsystem}
\end{figure}

The diagram above illustrates the use cases composing the household management subsystem. The \textit{User / Member} actor represents both an authenticated user without a household (interacting with creation and join use cases) and a standard household member (viewing members, leaving, and retrieving the invitation code of their household). The \textit{Admin} actor represents a household administrator with elevated privileges, capable of refreshing the invitation code and removing other members.

\subsection{Use Case Templates}

% ===== UC-1: Create Household =====
\subsubsection{Create Household UC Template}
\ucheader{UC \#1: Create Household}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Create a new household and automatically become its administrator. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Authenticated User \\ \hline
\textbf{Pre-conditions} & The user is authenticated and does not already belong to a household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user specifies a unique name for the new household.
    \item The system validates that the name is non-empty and not already in use.
    \item The system generates a unique invitation code for the household.
    \item The system persists the household and adds the user as its only member and administrator.
    \item The system returns the household details.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & A new household is persisted, and the user is assigned the administrator role. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user already belongs to a household. The system rejects the request.
    \item[\textbf{A2.}] The chosen household name already exists. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-2: Join Household =====
\subsubsection{Join Household UC Template}
\ucheader{UC \#2: Join Household}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Join an existing household using a valid invitation code. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Authenticated User \\ \hline
\textbf{Pre-conditions} & The user is authenticated and does not already belong to a household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user provides a household invitation code.
    \item The system validates the code against existing households.
    \item The system adds the user to the household as a standard member.
    \item The system returns the household details.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The user is added to the household as a standard member. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user already belongs to a household. The system rejects the request.
    \item[\textbf{A2.}] The invitation code is invalid or expired. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-3: View Members =====
\subsubsection{View Members UC Template}
\ucheader{UC \#3: View Members}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve the list of all members in the current household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user requests the list of household members.
    \item The system retrieves all members of the household along with their roles (admin or standard) and profile information.
    \item The system returns the list of members.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The list of household members is returned to the user. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user does not belong to a household. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-4: Manage Invitation Code =====
\subsubsection{Manage Invitation Code UC Template}
\ucheader{UC \#4: Manage Invitation Code}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve the current invitation code or generate a new one to control household access. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member, Household Admin \\ \hline
\textbf{Pre-conditions} & The user must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user requests to view the household's current invitation code.
    \item The system retrieves and returns the current valid invitation code.
    \item (Admin only) The administrator requests to refresh the invitation code.
    \item (Admin only) The system generates a new unique invitation code, invalidating the previous one.
    \item (Admin only) The system returns the newly generated invitation code.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The current or newly generated invitation code is returned to the user. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] A non-admin member attempts to refresh the code. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-5: Remove Member =====
\subsubsection{Remove Member UC Template}
\ucheader{UC \#5: Remove Member}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Remove a member from the household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Admin \\ \hline
\textbf{Pre-conditions} & The user must be an administrator of the household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The administrator selects a member to remove from the household.
    \item The system validates that the requester is an admin and the target user is a member of the same household.
    \item The system verifies that the administrator is not attempting to remove themselves.
    \item The system removes the target user from the household.
    \item The system returns the updated household details.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The target user is removed from the household. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The requester is not an admin. The system rejects the request.
    \item[\textbf{A2.}] The administrator attempts to remove themselves. The system instructs them to use the "Leave Household" flow instead.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-6: Leave Household =====
\subsubsection{Leave Household UC Template}
\ucheader{UC \#6: Leave Household}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Voluntarily leave the current household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user requests to leave their current household.
    \item The system removes the user from the household members list.
    \item If the user was the sole administrator and other members remain, the system automatically transfers the admin role to another member.
    \item If the leaving user was the last member, the system deletes the household.
    \item The system confirms the successful departure.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The user is removed from the household. The household may be deleted or have its admin reassigned if necessary. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user does not belong to a household. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

\section{Financial Tracking}

\subsection{Use Case Diagram}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    usecase/.style={
        ellipse, draw=black, fill=gray!20,
        minimum width=3.8cm, minimum height=1.3cm,
        align=center, font=\small\sffamily
    },
    sysbox/.style={
        rectangle, draw=black, rounded corners=3pt,
        minimum width=10cm, minimum height=11.5cm
    },
    relation/.style={thick, draw=black},
    include/.style={->, dashed, thick, draw=black!70}
]

% System boundary
\node[sysbox, label={[font=\sffamily\bfseries]above:Financial Tracking Subsystem}] (sys) at (0,0) {};

% ---- Stick-figure Actor ----
\begin{scope}[shift={(-7,0)}]
    % Head
    \draw[thick] (0, 0.55) circle (0.2cm);
    % Body
    \draw[thick] (0, 0.35) -- (0, -0.15);
    % Arms
    \draw[thick] (-0.3, 0.2) -- (0, 0.1) -- (0.3, 0.2);
    % Legs
    \draw[thick] (-0.25, -0.5) -- (0, -0.15) -- (0.25, -0.5);
    % Label
    \node[font=\sffamily\bfseries, below] at (0, -0.55) {Household};
    \node[font=\sffamily\bfseries, below] at (0, -0.85) {Member};
\end{scope}
\coordinate (actor) at (-7, 0);

% Use cases
\node[usecase] (uc1) at (0, 4)   {Log Expense};
\node[usecase] (uc2) at (0, 2)   {View Expenses};
\node[usecase] (uc3) at (0, 0)   {View Debts};
\node[usecase] (uc4) at (0,-2)   {Settle Debt};
\node[usecase] (uc5) at (0,-4)   {View Settlements};

% Internal use cases (included)
\node[usecase] (uc6) at (5.5, 3)  {Calculate\\Shares};
\node[usecase] (uc7) at (5.5, 0)  {Update Debt\\Ledger};

% Actor connections
\draw[relation] (actor) -- (uc1);
\draw[relation] (actor) -- (uc2);
\draw[relation] (actor) -- (uc3);
\draw[relation] (actor) -- (uc4);
\draw[relation] (actor) -- (uc5);

% Include relationships
\draw[include] (uc1) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc6);
\draw[include] (uc1) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc7);
\draw[include] (uc4) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc7);

\end{tikzpicture}}%
\caption{Use Case Diagram: Financial Tracking Subsystem}
\end{figure}

The diagram above illustrates the use cases composing the expense management subsystem. The \textit{Household Member} actor interacts directly with five boundary use cases. The two internal use cases, \textit{Calculate Shares} and \textit{Update Debt Ledger}, are included automatically by the system during expense creation and debt settlement respectively, and are never invoked directly by the user.

\subsection{Use Case Templates}

% ===== UC-1: Log Expense =====
\subsubsection{Log Expense UC Template}
\ucheader{UC \#1: Log Expense}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Record a shared expense and automatically update all affected debts. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor provides a description, total amount, a split type (equal, proportional, exact amounts, or adjustment), and the list of involved members.
    \item The system validates that the amount is positive and the participant list is non-empty.
    \item The system selects the appropriate splitting strategy and computes each member's share (include: \textit{Calculate Shares}).
    \item The expense and all individual shares are persisted atomically.
    \item For each participant, the system updates the debt ledger (include: \textit{Update Debt Ledger}).
    \item The system returns the created expense with all computed shares.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The expense record and its computed shares are persisted. All affected debt balances are updated via the netting algorithm. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] If the split configuration is invalid (e.g., exact amounts do not sum to the total), the system rejects the request and no data is persisted.
    \item[\textbf{A2.}] If a participant does not exist in the system, the entire operation is rolled back.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-2: View Expenses =====
\subsubsection{View Expenses UC Template}
\ucheader{UC \#2: View Expenses}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve a filtered list or an aggregated overview of household expenses. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor requests the expense list, optionally providing filters: transaction role, date range, and description keyword.
    \item The system constructs a dynamic query from the provided filters, scoped to the actor's household.
    \item The system returns the matching expenses, each including the payer's identity and the individual shares breakdown.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The requested expense data is returned to the actor. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The actor requests the household expense summary instead. The system returns the total amount and count of expenses for the current month.
    \item[\textbf{A2.}] The actor requests their personal financial summary instead. The system computes the net cash-flow for the current month and returns the aggregate value.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-3: View Debts =====
\subsubsection{View Debts UC Template}
\ucheader{UC \#3: View Debts}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve a filtered list or an aggregated overview of the actor's debts. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor requests their debts, specifying a role filter (creditor, debtor, or both) and an optional counterpart filter.
    \item The system queries the debt ledger for the actor's household, applying the requested filters.
    \item The system returns each matching debt with the counterpart's identity, the actor's role, and the outstanding amount.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The requested debt data is returned to the actor. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The actor requests the debt summary instead. The system computes and returns the aggregate totals of amounts owed by and to the actor.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-4: Settle Debt =====
\subsubsection{Settle Debt UC Template}
\ucheader{UC \#4: Settle Debt}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Record a payment that reduces or eliminates an outstanding debt. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated. An outstanding debt must exist where the actor is the debtor. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor selects an outstanding debt and provides a settlement amount and an optional description.
    \item The system validates that the actor is the debtor, that the creditor matches, and that the amount does not exceed the current balance.
    \item The system creates a settlement record and reduces the debt balance accordingly (include: \textit{Update Debt Ledger}).
    \item If the debt is fully covered, the balance is set to zero rather than deleting the record.
    \item The system returns the created settlement record.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & A settlement record is persisted and the debt balance is reduced. If fully settled, the balance is set to zero. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The actor is not the debtor on the selected debt. The system rejects the request.
    \item[\textbf{A2.}] The specified creditor does not match the debt record. The system rejects the request.
    \item[\textbf{A3.}] The settlement amount exceeds the current balance. The system rejects the request to prevent negative balances.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-5: View Settlements =====
\subsubsection{View Settlements UC Template}
\ucheader{UC \#5: View Settlements}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve a filtered list of settlement transactions within the household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The actor must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The actor requests the settlement history, optionally providing filters: transaction role, date range, and description keyword.
    \item The system constructs a dynamic query from the provided filters, scoped to the actor's household.
    \item The system returns the matching settlements, each including the counterpart's identity, the settled amount, timestamp, and description.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The requested settlement data is returned to the actor. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] No settlements match the provided filters. The system returns an empty list.
\end{itemize} \\ \hline
\end{longtable}

\section{Shopping List Management}

\subsection{Use Case Diagram}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    usecase/.style={
        ellipse, draw=black, fill=gray!20,
        minimum width=3.8cm, minimum height=1.3cm,
        align=center, font=\small\sffamily
    },
    sysbox/.style={
        rectangle, draw=black, rounded corners=3pt,
        minimum width=10cm, minimum height=9.5cm
    },
    relation/.style={thick, draw=black},
    include/.style={->, dashed, thick, draw=black!70}
]

% System boundary
\node[sysbox, label={[font=\sffamily\bfseries]above:Shopping List Management Subsystem}] (sys) at (0,0) {};

% ---- Stick-figure Actor ----
\begin{scope}[shift={(-7, 0)}]
    % Head
    \draw[thick] (0, 0.55) circle (0.2cm);
    % Body
    \draw[thick] (0, 0.35) -- (0, -0.15);
    % Arms
    \draw[thick] (-0.3, 0.2) -- (0, 0.1) -- (0.3, 0.2);
    % Legs
    \draw[thick] (-0.25, -0.5) -- (0, -0.15) -- (0.25, -0.5);
    % Label
    \node[font=\sffamily\bfseries, below] at (0, -0.55) {Household};
    \node[font=\sffamily\bfseries, below] at (0, -0.85) {Member};
\end{scope}
\coordinate (actor) at (-7, 0);

% Use cases
\node[usecase] (uc1) at (0, 3)   {Create Shopping List};
\node[usecase] (uc2) at (0, 1)   {Update Shopping List};
\node[usecase] (uc3) at (0,-1)   {View Shopping Lists};
\node[usecase] (uc4) at (0,-3)   {Delete Shopping List};

% Internal use case
\node[usecase] (uc5) at (5.5, 1)  {Calculate\\List Status};

% Actor connections
\draw[relation] (actor) -- (uc1);
\draw[relation] (actor) -- (uc2);
\draw[relation] (actor) -- (uc3);
\draw[relation] (actor) -- (uc4);

% Include relationships
\draw[include] (uc2) -- node[above, font=\scriptsize\sffamily, sloped] {\guillemotleft include\guillemotright} (uc5);

\end{tikzpicture}}%
\caption{Use Case Diagram: Shopping List Management Subsystem}
\end{figure}

The diagram above illustrates the use cases composing the shopping list management subsystem. The \textit{Household Member} actor interacts directly with four boundary use cases for full CRUD capabilities on shared shopping lists. The internal use case, \textit{Calculate List Status}, is included automatically by the system during list updates to determine if the list is completely bought, partially bought, or not started.

\subsection{Use Case Templates}

% ===== UC-1: Create Shopping List =====
\subsubsection{Create Shopping List UC Template}
\ucheader{UC \#1: Create Shopping List}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Create a new shared shopping list for the household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user provides a name and a set of items to buy.
    \item The system creates a new shopping list associated with the user's household, initialized with a \texttt{NOT\_STARTED} status.
    \item The system persists the shopping list and returns its details.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & A new shopping list is persisted and available to the household. \\ \hline
\end{longtable}

% ===== UC-2: Update Shopping List =====
\subsubsection{Update Shopping List UC Template}
\ucheader{UC \#2: Update Shopping List}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Mark items as bought or modify the contents of an existing shopping list. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated, belong to an active household, and the target shopping list must exist. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user selects a shopping list and provides an updated array of item statuses (bought or not bought).
    \item The system validates that the user belongs to the household owning the list.
    \item The system applies the bought statuses to the list items.
    \item The system calculates the overall list status: \texttt{NOT\_STARTED} if none are bought, \texttt{COMPLETED} if all are bought, \texttt{IN\_PROGRESS} otherwise (include: \textit{Calculate List Status}).
    \item The system persists the changes and returns the updated shopping list.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The shopping list items and overall status are updated. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user does not belong to the household that owns the list. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}

% ===== UC-3: View Shopping Lists =====
\subsubsection{View Shopping Lists UC Template}
\ucheader{UC \#3: View Shopping Lists}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Retrieve the overview of all shopping lists within the household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated and belong to an active household. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user requests the overview of their household's shopping lists.
    \item The system retrieves all shopping lists associated with the household, including their items and current overall statuses.
    \item The system returns the lists to the user.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The user receives the shopping lists data. \\ \hline
\end{longtable}

% ===== UC-4: Delete Shopping List =====
\subsubsection{Delete Shopping List UC Template}
\ucheader{UC \#4: Delete Shopping List}
\arrayrulecolor{UCRule}
\begin{longtable}{|p{3.5cm}|p{10.3cm}|}
\hline
\rowcolor{UCRow} \textbf{Level} & User Goal \\ \hline
\textbf{Description} & Permanently delete a shopping list from the household. \\ \hline
\rowcolor{UCRow} \textbf{Actors} & Household Member \\ \hline
\textbf{Pre-conditions} & The user must be authenticated, belong to an active household, and the target shopping list must exist. \\ \hline
\rowcolor{UCRow} \textbf{Steps} &
\begin{enumerate}[nosep, leftmargin=*]
    \item The user selects a shopping list for deletion.
    \item The system validates that the user belongs to the household owning the list.
    \item The system removes the shopping list from the database.
    \item The system confirms the successful deletion.
\end{enumerate} \\ \hline
\textbf{Post-conditions} & The shopping list is deleted from the system. \\ \hline
\rowcolor{UCRow} \textbf{Alternative Steps} &
\begin{itemize}[nosep, leftmargin=2.5em]
    \item[\textbf{A1.}] The user does not belong to the household that owns the list. The system rejects the request.
\end{itemize} \\ \hline
\end{longtable}